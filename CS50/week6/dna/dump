	for i in range(len(seq)-3):
		STR = seq[i]
		for j in range(1,4):
			STR = STR + seq[i+j] 
		STRs.append(STR)
		
		
i=1
	j=1
	while True:
		while True:
			profile[i][j] = int(profile[i][j])
			j+=1
		i+=1
	
