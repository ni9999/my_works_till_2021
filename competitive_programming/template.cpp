#include <bits/stdc++.h>
using namespace std;

int main() {
    int t, i;
    cin >> t;
    for(i = 0 ; i < t ; i++){
        cin >> ;

    }
    return 0;
}


// to ignore after decimal point--> maxteams = (int) (math + prog) / 4;
